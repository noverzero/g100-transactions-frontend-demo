import {REGISTER_USER_SUCCESS, LOGIN_USER_SUCCESS, ERROR_MESSAGE, SUCCESS_MESSAGE, VERIFY_USER_SUCCESS} from "../types";
import axios from "../../axios";

export const userLogin = (user) => dispatch => {

}

export const userRegister = (user) => dispatch => {

}
